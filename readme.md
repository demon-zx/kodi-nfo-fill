# Инструмент заполнения файлов nfo для медиатеки Kodi

Как источник используется апи https://api.kinopoisk.dev, либо csv(tsv) файлы (только для сериалов). 
Так же можно выгрузить информацию из апи в csv(tsv).

# todo

- [x] сделать выгрузку в tsv
- [x] сделать поддержку *.part
- [x] для команды movie проверять, что -f 'Папка', и если это так, то класть nfo и постеры в нее
- [ ] смотреть movie.nfo и poster.nfo
- [x] при чтении из csv игнорировать кавычки у строк (подключить библиотеку работы с csv)

# Запуск

```
java -jar kodi-nfo-fill-1.0-SNAPSHOT-jar-with-dependencies.jar -t токен <command>
```